
  # Platafor for Hackathon Microsoft

  This is a code bundle for Plataforma de Feedback Inclusivo.

  ## Running the code
  

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  